% paper.tex: Reservoir Attention Network paper, NeurIPS 2026 submission wrapper.
%
% The substantive paper content lives in FINDINGS.md (the single canonical
% source, kept clean for the GitHub-Pages report build too). The paper-pdf.yml
% workflow preprocesses FINDINGS.md and runs `pandoc ... -o paper.tex.body`
% before pdflatex, so the entire body of the markdown lands here at \input time.
% paper.tex itself is the NeurIPS-2026 wrapper: documentclass, style options,
% packages, anonymization switch, title, and author block.
%
% Two builds from the same source via the \ifanon switch:
%   pdflatex paper.tex                                     -> named (preprint, no line numbers)
%   pdflatex -usepretex='\newif\ifanon\anontrue' paper.tex -> anonymized submission (line numbers)
%
% Build chain (paper-pdf.yml):
%   preprocess FINDINGS.md -> pandoc -t latex -o paper.tex.body
%                          -> latexmk -pdf paper.tex                   (named/preprint)
%                          -> latexmk -pdf -usepretex='...' paper.tex  (anonymized submission)

\documentclass{article}

% Anonymization switch; set true via -usepretex for the submission build.
\makeatletter
\@ifundefined{ifanon}{\newif\ifanon}{}
\makeatother

% Official NeurIPS 2026 style file. nonatbib disables natbib because the paper
% uses an unnumbered bullet-list References section, not natbib citations.
\ifanon
  \usepackage[nonatbib]{neurips_2026}
\else
  \usepackage[preprint,nonatbib]{neurips_2026}
\fi

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{hyperref}
\usepackage{url}
\usepackage{xcolor}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{longtable}
\usepackage{array}
% pandoc longtable column widths use \real on a \linewidth expression.
\usepackage{calc}
\providecommand{\real}[1]{#1}
\usepackage{caption}
\usepackage{enumitem}
\usepackage{verbatim}
\usepackage{graphicx}
% Bound EVERY \includegraphics to the text block (pandoc's standalone-template
% defaults). The CI pandoc emits bare \includegraphics{...} with no size args and
% no \pandocbounded wrapper, so without these Gin defaults a PNG whose natural
% size exceeds \linewidth runs off the right page edge (the cut-off-figures bug).
\makeatletter
\def\maxwidth{\ifdim\Gin@nat@width>\linewidth\linewidth\else\Gin@nat@width\fi}
\def\maxheight{\ifdim\Gin@nat@height>\textheight\textheight\else\Gin@nat@height\fi}
\makeatother
\setkeys{Gin}{width=\maxwidth,height=\maxheight,keepaspectratio}

% Map the math-style Unicode used in the body prose onto LaTeX equivalents.
\usepackage{newunicodechar}
\newunicodechar{≈}{\ensuremath{\approx}}
\newunicodechar{≠}{\ensuremath{\neq}}
\newunicodechar{×}{\ensuremath{\times}}
\newunicodechar{−}{\ensuremath{-}}
\newunicodechar{∈}{\ensuremath{\in}}
\newunicodechar{·}{\ensuremath{\cdot}}
\newunicodechar{ρ}{\ensuremath{\rho}}
\newunicodechar{λ}{\ensuremath{\lambda}}
\newunicodechar{→}{\ensuremath{\rightarrow}}
\newunicodechar{←}{\ensuremath{\leftarrow}}
\newunicodechar{…}{\ensuremath{\ldots}}
\newunicodechar{‑}{-}
\newunicodechar{≥}{\ensuremath{\geq}}
\newunicodechar{≤}{\ensuremath{\leq}}
\newunicodechar{≪}{\ensuremath{\ll}}
\newunicodechar{¼}{\ensuremath{\sfrac{1}{4}}}
\newunicodechar{⅒}{\ensuremath{\sfrac{1}{10}}}
\newunicodechar{⁰}{\textsuperscript{0}}
\newunicodechar{¹}{\textsuperscript{1}}
\newunicodechar{²}{\textsuperscript{2}}
\newunicodechar{³}{\textsuperscript{3}}
\newunicodechar{⁴}{\textsuperscript{4}}
\newunicodechar{⁵}{\textsuperscript{5}}
\newunicodechar{⁶}{\textsuperscript{6}}
\newunicodechar{⁷}{\textsuperscript{7}}
\newunicodechar{⁸}{\textsuperscript{8}}
\newunicodechar{⁹}{\textsuperscript{9}}
\newunicodechar{§}{\S}
% Greek + math symbols appearing in the prose (pdflatex has no glyphs for these
% in text mode; map to math-mode equivalents).
\newunicodechar{α}{\ensuremath{\alpha}}
\newunicodechar{β}{\ensuremath{\beta}}
\newunicodechar{γ}{\ensuremath{\gamma}}
\newunicodechar{δ}{\ensuremath{\delta}}
\newunicodechar{ε}{\ensuremath{\epsilon}}
\newunicodechar{σ}{\ensuremath{\sigma}}
\newunicodechar{μ}{\ensuremath{\mu}}
\newunicodechar{τ}{\ensuremath{\tau}}
\newunicodechar{φ}{\ensuremath{\phi}}
\newunicodechar{θ}{\ensuremath{\theta}}
\newunicodechar{ω}{\ensuremath{\omega}}
\newunicodechar{Σ}{\ensuremath{\Sigma}}
\newunicodechar{Δ}{\ensuremath{\Delta}}
\newunicodechar{Φ}{\ensuremath{\Phi}}
\newunicodechar{Ω}{\ensuremath{\Omega}}
\newunicodechar{Θ}{\ensuremath{\Theta}}
\newunicodechar{∇}{\ensuremath{\nabla}}
\newunicodechar{∂}{\ensuremath{\partial}}
\newunicodechar{∞}{\ensuremath{\infty}}
\newunicodechar{∘}{\ensuremath{\circ}}
\newunicodechar{⊆}{\ensuremath{\subseteq}}
\newunicodechar{⊂}{\ensuremath{\subset}}
\newunicodechar{±}{\ensuremath{\pm}}
\newunicodechar{‖}{\ensuremath{\Vert}}
\providecommand{\sfrac}[2]{#1/#2}

% pandoc longtable + hyperref shim (unnumbered tables anchor on counter 'none').
\makeatletter
\@ifundefined{c@none}{\newcounter{none}}{}
\makeatother
\providecommand{\theHnone}{none.\arabic{none}}

% pandoc emission shims.
\providecommand{\tightlist}{\setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
% pandoc's real \pandocbounded: scale an over-wide/over-tall image DOWN to fit the text block.
% Our previous no-op passthrough let wide figures (the diagrams, the bar charts) run off the
% page edge. This is pandoc's default-template definition.
\makeatletter
\newsavebox\pandoc@box
\newcommand*\pandocbounded[1]{%
  \sbox\pandoc@box{#1}%
  \Gscale@div\@tempa\textheight{\dimexpr\ht\pandoc@box+\dp\pandoc@box\relax}%
  \Gscale@div\@tempb\linewidth{\wd\pandoc@box}%
  \ifdim\@tempb\p@<\@tempa\p@\let\@tempa\@tempb\fi%
  \ifdim\@tempa\p@<\p@\scalebox{\@tempa}{\usebox\pandoc@box}%
  \else\usebox\pandoc@box\fi%
}
\makeatother
\usepackage{framed}
\definecolor{shadecolor}{RGB}{248,248,248}
\newenvironment{Shaded}{\begin{snugshade*}}{\end{snugshade*}}

\hypersetup{
  colorlinks=true,
  linkcolor=blue,
  urlcolor=blue,
  citecolor=blue,
}

\title{The Reservoir Attention Network:\\
  Cross-Pass State in Pretrained Transformers\\
  via Content-Addressable Reservoir Injection}

\ifanon
  \author{Anonymous Authors}
\else
  \author{%
    Emma Leonhart\\
    \texttt{contact@emmaleonhart.com}%
  }
\fi

\begin{document}
\maketitle

% The body comes from `pandoc <preprocessed FINDINGS.md> -t latex -o
% paper.tex.body`, generated by paper-pdf.yml before pdflatex runs.
\input{paper.tex.body}

\end{document}
