"""Installer for reservoir-agent models — registry, download, and a stateful console.

``python -m reservoir.installer`` lists the project's reservoir-agent models (default =
the recommended best), downloads the chosen one from the Hugging Face Hub, and launches an
interactive console that carries reservoir state across turns.
"""
